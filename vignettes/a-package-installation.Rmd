---
title: "a - package installation"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{a - package installation}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(finddxtemplate)
```

<!-- This vignette is generated by fusen: do not edit by hand -->

<!--
 You need to run the 'description' chunk in the '0-dev_history.Rmd' file before continuing your code there.
-->


# Install the package from the command line

```{r eval = FALSE}
if (requireNamespace("remotes")){install.packages("remotes")}
remotes::install_local(path = "path/to/finddxtemplate_0.1.0.tar.gz", 
                       repos = "http://cran.rstudio.com", 
                       type = "source")
```

# Install the package graphically

```{r eval = FALSE}
if (requireNamespace("remotes")){install.packages("remotes")}
# a window opens and asks to choose the package in tar.gz format
remotes::install_local(path = file.choose(), 
                       repos = "http://cran.rstudio.com", 
                       type = "source")
```

